#include "recursion.h"

/**
 * main - entry point to test recursion algorithms
 * 
 * Return: 0 on success
 */
int main (void)
{
	float num = 0;

	num = _pow_recursion(1, 2);
	printf("%f\n", num);

	return (0);
}